- name: Install Docker packages
  apt:
    name:
      - docker.io
      - docker-compose-v2
    state: present
    update_cache: true

- name: Enable Docker service
  systemd:
    name: docker
    enabled: true
    state: started
